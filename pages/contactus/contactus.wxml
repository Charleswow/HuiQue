<view>
    <view class="loading-modal" wx:if="{{showornot}}">
        <view class="loading-text-wrap">
            <image src="../images/loading.gif" class="load_img"></image>
            <view class="loading-text">loading...</view>
        </view>
    </view>
    <view wx:else>
        <view id='a1'>
            Q&A
        </view >
        <view id='c1' wx:for="{{QA}}">
            
            <view  id='a3'>{{item.question}}</view>
            <view id='a4'>{{item.answer}}</view>
        </view>
        <button id='b1' open-type="contact">我要反馈</button>
    </view>
</view>